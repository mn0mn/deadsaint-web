// Cart state/logic depends heavily on the backend you pick (Shopify cart API,
// custom cart table, or client-only state + a checkout API). This is a
// placeholder page to wire up once that decision is made.

export default function CartPage() {
  return (
    <section className="cart">
      <h1>Your cart</h1>
      <p>Cart functionality depends on the backend/checkout provider — TBD.</p>
    </section>
  );
}
